# 种子数据已成功插入

## ✅ 数据状态

种子数据已成功清理并重新插入到数据库中：

- ✅ **5 个用户**
  - admin@vioflow.com (管理员)
  - sarah@vioflow.com (成员)
  - mike@vioflow.com (成员)
  - alex@vioflow.com (成员)
  - sales@vioflow.com (销售)

- ✅ **5 个项目**
  - 2412_Nike_AirMax_Holiday (Nike) - active
  - 2501_Spotify_Wrapped_Asia (Spotify) - active
  - 2411_Netflix_Docu_S1 (Netflix) - finalized
  - 2410_Porsche_911_Launch (Porsche) - delivered
  - 2409_Apple_Event_Launch (Apple) - active

- ✅ **4 个视频**
- ✅ **8 个标签**
- ✅ **3 个项目成员关系**
- ✅ **2 个交付记录**
- ✅ **2 个通知**

## 🔑 测试账号

- **管理员账号**：`admin@vioflow.com` / `admin`
- **成员账号**：`sarah@vioflow.com` / `admin`

## 🧪 验证数据

### 方法 1：使用检查脚本

```bash
cd backend
npm run check-db
```

### 方法 2：使用 API 测试脚本

```bash
cd backend
./test-api.sh
```

### 方法 3：通过前端验证

1. 启动前端服务：
   ```bash
   npm run dev
   ```

2. 访问 http://localhost:3009

3. 使用 `admin@vioflow.com` / `admin` 登录

4. 应该能看到：
   - 5 个项目列表
   - 4 个视频
   - 8 个标签
   - 通知信息

## 🔄 重新运行种子数据

如果需要重新插入数据：

```bash
cd backend
npm run seed:reset
```

这会：
1. 清理所有现有数据
2. 重新插入种子数据

## ⚠️ 如果前端看不到数据

如果数据已插入但前端看不到，检查：

1. **后端服务是否运行**：
   ```bash
   curl http://localhost:3002/health
   ```

2. **前端是否正确连接后端**：
   - 检查浏览器控制台（F12）
   - 查看 Network 标签页
   - 确认 API 请求是否成功

3. **用户是否已登录**：
   - 检查 localStorage 中是否有 `auth_token`
   - 确认 API 请求包含 Authorization 头

4. **API 地址配置**：
   - 前端应该使用 `http://localhost:3002/api`
   - 检查 `src/api/client.ts` 中的配置

## 📝 相关命令

- `npm run check-db` - 检查数据库状态
- `npm run seed` - 运行种子数据（不清理）
- `npm run seed:simple` - 运行简化版种子数据
- `npm run seed:reset` - 清理并重新插入种子数据
- `./test-api.sh` - 测试所有 API 端点

